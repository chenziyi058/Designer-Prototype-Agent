# 架构说明

`Prototype Engineer Orchestrator` 以 `ProjectSpec` 为唯一输入，依次调用需求、架构、硬件、BOM、协议、固件、Python、界面、验证、测试与文档能力。Provider 层只负责模糊推理；Schema、存储、影响分析、成本、电气和协议一致性检查由普通程序执行。

核心修改先形成新需求版本，再使受影响资产失效并重新生成。每个文件记录来源版本和校验和，使前端可以解释“Agent 做了什么、为何这样做、哪些信息仍不确定”。
